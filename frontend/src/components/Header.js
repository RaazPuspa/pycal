import React from 'react';

const Header = () => {
  return (
    <header className="app-header">
      <div className="app-header-container">
        <p className="app-title">pyCal</p>
        <p className="app-sub-title">
          the Python django + ReactJS calculator
        </p>
      </div>
    </header>
  );
}

export default Header;
